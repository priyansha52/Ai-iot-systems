#define BLYNK_TEMPLATE_ID "TMPL3FxhBUKDM"
#define BLYNK_TEMPLATE_NAME "TEMPERATURE  AND HUMIDITY  SENSOR SYSTEM"
#define BLYNK_AUTH_TOKEN    "7raj3x4_66pPDtxByiebwo3KKTV5c6VN"

#include <WiFi.h>
#include <BlynkSimpleEsp32.h>
#include <DHT.h>

// ===== WiFi Credentials =====
char ssid[] = "realme10Pro5G";
char pass[] = "use your network";

// ===== DHT Settings =====
#define DHTPIN 4
#define DHTTYPE DHT11

DHT dht(DHTPIN, DHTTYPE);

BlynkTimer timer;


// ===== Function to Send Data =====
void sendSensor()
{
float humidity = dht.readHumidity();
float temperature = dht.readTemperature();

// Check if sensor failed
if (isnan(humidity) || isnan(temperature))
{
Serial.println("Failed to read from DHT11!");
return;
}

// Print in Serial Monitor
Serial.print("Temperature: ");
Serial.print(temperature);
Serial.println(" °C");

Serial.print("Humidity: ");
Serial.print(humidity);
Serial.println(" %");

Serial.println("----------------------");

// Send data to Blynk
Blynk.virtualWrite(V0, temperature);
Blynk.virtualWrite(V1, humidity);
}

void setup()
{
// Serial Monitor
Serial.begin(115200);

// Start DHT
dht.begin();

// Connect to WiFi & Blynk
Blynk.begin(BLYNK_AUTH_TOKEN, ssid, pass);

// Send sensor data every 2 seconds
timer.setInterval(2000L, sendSensor);

Serial.println("System Started");
}

void loop()
{
Blynk.run();
timer.run();
}
